import { DefaultTheme } from 'styled-components';

import { colors } from './colors';
import { fonts } from './fonts';

const navedexTheme: DefaultTheme = {
  colors,
  fonts,
};

export { navedexTheme };
